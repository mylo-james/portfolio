import styled from 'styled-components';

const ProjectsSty = styled.div`
    height: 500px;
    position: relative;

    .anchor {
        position: absolute;
        top: -50px;
    }
`;

export default ProjectsSty;
